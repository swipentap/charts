apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: keycloak
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  source:
    repoURL: https://swipentap.github.io/charts
    chart: keycloak
    targetRevision: {{ .Values.keycloakChartRevision | quote }}
    helm:
      releaseName: keycloak
      values: |
        ingress:
          enabled: true
          ingressClassName: traefik
          annotations:
            traefik.ingress.kubernetes.io/router.entrypoints: web,websecure
          rules:
            - host: auth.{{ .Values.global.domain }}
              paths:
                - path: /
                  pathType: Prefix
          tls:
            - hosts:
                - auth.{{ .Values.global.domain }}
              secretName: {{ .Values.global.tlsSecret }}
        route:
          enabled: false
        extraEnv: |
          - name: KEYCLOAK_USER
            value: admin
          - name: KEYCLOAK_PASSWORD
            value: admin
  destination:
    server: https://kubernetes.default.svc
    namespace: keycloak
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
    - CreateNamespace=true
