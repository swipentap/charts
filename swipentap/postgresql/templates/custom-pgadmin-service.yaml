{{- if .Values.customPgadmin.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: pgadmin
  namespace: {{ .Values.customPgadmin.namespace }}
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: pgadmin
{{- end }}
