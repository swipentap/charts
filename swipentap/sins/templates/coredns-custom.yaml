apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
data:
  {{ printf "%s.server" .Values.domain }}: |
    {{ .Values.domain }}:53 {
        forward . {{ include "sins.fullname" . }}.{{ include "sins.namespace" . }}.svc.cluster.local:5353
    }
